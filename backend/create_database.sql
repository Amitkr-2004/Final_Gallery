-- SQL script to create the photogallery database
-- Run this in PostgreSQL to create the database

CREATE DATABASE photogallery;

-- Optional: Grant privileges (if needed)
-- GRANT ALL PRIVILEGES ON DATABASE photogallery TO postgres;
